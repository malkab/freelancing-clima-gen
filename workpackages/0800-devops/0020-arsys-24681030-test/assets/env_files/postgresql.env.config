# Ver en vault.

# Conexión a PG.
PGHOST=XXX
PGPORT=XXX
PGUSER=XXX
PGDATABASE=XXX
PGPASSWORD=XXX

# Para la inicialización del container.
POSTGRES_PASSWORD=${PGPASSWORD}

# Conexión para Martin.
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}