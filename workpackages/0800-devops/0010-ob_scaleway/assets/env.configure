# VER el vault.

# PostgreSQL
PGHOST=XXX
PGPORT=XXX
PGUSER=XXX
PGDATABASE=XXX
PGPASSWORD=XXX

# Para la imagen de PostgreSQL
POSTGRES_PASSWORD=${PGPASSWORD}

# Para Martin
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}