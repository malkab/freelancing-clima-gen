# VER el vault.

# PostgreSQL
PGHOST=XXX
PGPORT=XXX
PGUSER=XXX
PGDATABASE=XXX
PGPASSWORD=XXX

# Para la imagen de PostgreSQL
POSTGRES_PASSWORD=${PGPASSWORD}

# Para Martin
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# TLS / Let's Encrypt
LETSENCRYPT_DOMAIN=XXX
LETSENCRYPT_DOMAIN_WWW=
LETSENCRYPT_EMAIL=XXX
LETSENCRYPT_STAGING=0

# Despliegue
SSH_HOST=XXX
DEPLOYMENT_FOLDER=XXX